import 'package:flutter/material.dart';

class NetworkData {
  String title;
  String subtitle;
  Icon icon;

  NetworkData({
    required this.title,
    required this.subtitle,
    required this.icon});
}